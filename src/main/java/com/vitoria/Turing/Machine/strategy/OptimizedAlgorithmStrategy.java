package com.vitoria.Turing.Machine.strategy;

import java.util.LinkedList;

public class OptimizedAlgorithmStrategy implements PrimalityStrategy {
    @Override
    public boolean isPrime(LinkedList<Character> binaryNumber) {
        return false;
    }
}
